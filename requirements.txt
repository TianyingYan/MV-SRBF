# Direct dependencies validated in the `pytorch` Conda environment.
# Python: 3.13.13
# PyTorch wheels are platform/CUDA specific. The validated build is CUDA 12.4:
# torch 2.6.0+cu124 and torchvision 0.21.0+cu124. Install matching wheels
# from the PyTorch index first when they are not available from the active index.
torch==2.6.0
torchvision==0.21.0
numpy==2.4.6
Pillow==12.2.0
PyYAML==6.0.3
tifffile==2026.6.1
safetensors==0.8.0

# Backbone providers.
clip @ git+https://github.com/openai/CLIP.git
open_clip_torch==3.3.0
timm==1.0.27
transformers==5.12.0

# Evaluation, tracking, model analysis, and visualization.
faiss-cpu==1.14.3
trackio==0.27.0
fvcore==0.1.5.post20221221
matplotlib==3.11.0
grad-cam==1.5.5
scikit-learn==1.9.0
